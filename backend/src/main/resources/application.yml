analysis:
  base-url: "http://localhost:8000"

spring:
  profiles:
    active: h2   # h2 <-> mysql 변경해서 사용

  jackson:
    property-naming-strategy: SNAKE_CASE

  config:
    import: optional:application-secret.yml